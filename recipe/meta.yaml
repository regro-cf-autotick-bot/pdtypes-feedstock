{% set name = "pdtypes" %}
{% set version = "0.2.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pdtypes-{{ version }}.tar.gz
  sha256: 5a40229e82cc79c65500134267b8870943ab8126726db90bc7fb778e2edb2a62

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.7,<4.0
    - poetry >=1.1.12
  run:
    - pandas >=1.3,<2.0
    - python >=3.7,<4.0

test:
  imports:
    - pdtypes
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/pwwang/pdtypes
  summary: Show data types for pandas data frames in terminal and notebooks
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - AhmedAliStats
